/*first c program*/

#include <stdio.h> // 引入標準輸入輸出函式庫


int main() { // 每個C語言程式裡面都必須有一個main()函式 在main() 以一組大括號{}把要執行的程式碼包起來
    
    /* 每次都必須以分號; 做結尾 */
    printf("Hello World! \n"); // \n 是換行符號 沒有 \n → 下一次輸出會 接在同一行後面 有了 \n → 下一次輸出會 從下一行開始
}
